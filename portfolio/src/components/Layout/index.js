import Sidebar from '../sidebar'; // Assuming sidebar.js is in the parent directory
import './index.scss';

const Layout = () => {
    return (
        <>
            <Sidebar />
        </>
    );
}

export default Layout;
